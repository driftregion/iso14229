version: 2.1

jobs:
  test-linux:
    machine:
      image: ubuntu-2204:current   # Dedicated VM; 24.04 is fine if/when available
    resource_class: medium
    steps:

      - checkout

      - run:
          name: Install kernel extras + tools
          command: |
            sudo apt-get update
            # Extras usually contain vcan.ko and can-isotp.ko on Ubuntu generic kernels
            sudo apt-get install -y "linux-modules-extra-$(uname -r)" can-utils build-essential curl

      - run:
          name: Install Bazel (Bazelisk)
          command: |
            set -euxo pipefail
            curl -L https://github.com/bazelbuild/bazelisk/releases/latest/download/bazelisk-linux-amd64 -o bazelisk
            sudo mv bazelisk /usr/local/bin/bazel
            sudo chmod +x /usr/local/bin/bazel
            bazel version


      - run:
          name: Enable SocketCAN (vcan) + try ISO-TP
          command: |
            set -euxo pipefail
            sudo modprobe can || true
            sudo modprobe can_raw || true
            sudo modprobe vcan || true
            sudo ip link add dev vcan0 type vcan || true
            sudo ip link set vcan0 up
            ip -details link show vcan0

      # Optional smoke test (only if kernel ISO-TP succeeded)
      - run:
          name: ISO-TP smoke test (optional)
          when: on_success
          command: |
            set -euxo pipefail
            isotprecv -s 0x700 -d 0x701 -l vcan0 &
            sleep 1
            printf "01 0C\n" | isotpsend -s 0x701 -d 0x700 vcan0
            sleep 1
            pkill isotprecv || true

      - run:
          name: coverage
          command: |
            set -euxo pipefail
            tools/coverage_run.sh
            cp "$(bazel info output_path)/_coverage/_coverage_report.dat" coverage.lcov

      - run:
          name: coverage upload
          command: |
            curl -Os https://cli.codecov.io/latest/linux/codecov
            chmod +x codecov
            ./codecov --verbose upload-process --fail-on-error -t $CODECOV_TOKEN -F unittests --name "CircleCI Unit Tests Coverage"

  build-examples:
    machine:
      image: ubuntu-2204:current
    resource_class: medium
    steps:
      - checkout

      - run:
          name: Install dependencies
          command: |
            sudo apt-get update
            sudo apt-get install -y "linux-modules-extra-$(uname -r)" can-utils build-essential libmbedtls-dev

      - run:
          name: Enable SocketCAN (vcan)
          command: |
            set -euxo pipefail
            sudo modprobe can || true
            sudo modprobe can_raw || true
            sudo modprobe vcan || true
            sudo ip link add dev vcan0 type vcan || true
            sudo ip link set vcan0 up
            ip -details link show vcan0

      - run:
          name: Build and test linux_rdbi_wdbi
          command: |
            cd examples/linux_rdbi_wdbi
            ./test.sh

      - run:
          name: Build linux_server
          command: |
            cd examples/linux_server
            make

      - run:
          name: Build and test linux_server_0x27
          command: |
            cd examples/linux_server_0x27
            ./test.sh

      - run:
          name: Build and test DoIP client example
          command: |
            cd examples/doip_client
            ./test.sh

workflows:
  version: 2
  build:
    jobs:
      - test-linux
      - build-examples